# Contributing

Use CPU tests before rebuilding the notebook. Keep the upload-only/TXT-only UI.
Do not introduce unreviewed model swaps or accuracy claims. For real TPU reports,
include model revision, dtype, environment, timing boundaries and whether the job
resumed. Compare a consented audio sample against a human reference before
claiming recognition quality. Keep speech data out of public issues by default.
