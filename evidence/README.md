# Evidence — BreezeSprint26 0.1.1

191 local tests passed. `cpu-tests.xml` and `cpu-tests.log` are the final test run.
`regression-reproduction.json` records the exact old exception and the fixed
metadata-only result on a fixture with documented special IDs and synthetic
ordinary byte vocabulary. `validation.json` states the limits of each check.

Full publisher-vocabulary download and physical TPU inference have NOT been
completed in this build environment. The source includes a real metadata-only
network check for CI and Colab, not a fabricated pre-recorded passing result.
